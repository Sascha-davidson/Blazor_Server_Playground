namespace Playground.Templating.Enums
{
    public enum CellType
    {
        // <summary>
        /// A standard table cell (`<td>`) used by default.
        /// </summary>
        Default,

        /// <summary>
        /// A two-column cell: one with the currency symbol (e.g., €), and one with the value.
        /// </summary>
        Currency,
    }
}
